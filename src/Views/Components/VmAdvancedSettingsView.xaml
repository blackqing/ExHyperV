<UserControl xmlns:properties="clr-namespace:ExHyperV.Properties" x:Class="ExHyperV.Views.VmAdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="AdvancedToggleStyle" TargetType="ui:ToggleSwitch" BasedOn="{StaticResource {x:Type ui:ToggleSwitch}}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 头部 -->
        <Grid Grid.Row="0" Margin="24,16,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ui:FontIcon Glyph="&#xE713;" FontFamily="{DynamicResource SegoeFluentIcons}" FontSize="22" Margin="0,0,12,0" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Static properties:Resources.VmAdvanced_Title}" FontSize="20" FontWeight="Normal" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                </StackPanel>
                <TextBlock Text="{x:Static properties:Resources.VmAdvanced_Hint}" FontSize="13" Opacity="0.6" Margin="0,6,0,0" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
            <ui:Button Grid.Column="1" Appearance="Transparent" Padding="8" Command="{Binding GoBackToDashboardCommand}">
                <ui:SymbolIcon Symbol="ArrowLeft24" FontSize="20"/>
            </ui:Button>
        </Grid>

        <!-- 内容 -->
        <ScrollViewer Grid.Row="1" Padding="24,12,24,24" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
            <Border Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Margin="0,0,0,32">
                <StackPanel>
                    <!-- 控制台支持 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{x:Static properties:Resources.VmAdvanced_ConsoleTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{x:Static properties:Resources.VmAdvanced_ConsoleHint}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding IsConsoleSupportEnabled, Mode=TwoWay}"
                                         IsEnabled="{Binding SelectedVm.CanToggleConsoleSupport}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 基本会话默认分辨率 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{x:Static properties:Resources.VmAdvanced_ResolutionTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{x:Static properties:Resources.VmAdvanced_ResolutionHint}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                                    IsEnabled="{Binding SelectedVm.IsRunning, Converter={StaticResource InverseBooleanConverter}}">
                            <ComboBox MinWidth="150" IsEditable="True"
                                      ItemsSource="{Binding VideoResolutionOptions}"
                                      Text="{Binding SelectedVideoResolution, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <ui:Button Content="{x:Static properties:Resources.VmAdvanced_Apply}" Margin="8,0,0,0"
                                       Appearance="Primary"
                                       Command="{Binding ApplyVideoResolutionCommand}"/>
                        </StackPanel>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 启动时 NumLock -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{x:Static properties:Resources.VmAdvanced_NumLockTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding IsBootNumLockEnabled, Mode=TwoWay}"
                                         IsEnabled="{Binding SelectedVm.IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 断开时锁定 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{Binding VmAdvancedLockTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VmAdvancedLockDesc}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding LockOnDisconnect, Mode=TwoWay}"
                                         IsEnabled="{Binding LockOnDisconnectAvailable}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 虚拟机休眠 -->
                    <Grid Margin="24,16"
                          IsEnabled="{Binding SelectedVm.IsRunning, Converter={StaticResource InverseBooleanConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{Binding VmAdvancedHibernationTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VmAdvancedHibernationDesc}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding EnableHibernation, Mode=TwoWay}"
                                         IsEnabled="{Binding EnableHibernationAvailable}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 重启时改为关机 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{Binding VmAdvancedTurnOffTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VmAdvancedTurnOffDesc}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding TurnOffOnGuestRestart, Mode=TwoWay}"
                                         IsEnabled="{Binding TurnOffOnGuestRestartAvailable}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 完整 SCSI 命令集 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{Binding VmAdvancedFullScsiTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VmAdvancedFullScsiDesc}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding AllowFullScsiCommandSet, Mode=TwoWay}"
                                         IsEnabled="{Binding AllowFullScsiCommandSetAvailable}"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ui:ThemeResource CardStrokeColorDefaultBrush}" Margin="24,0"/>

                    <!-- 电池状态透传 -->
                    <Grid Margin="24,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="{Binding VmAdvancedBatteryTitle}" FontSize="14" Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding VmAdvancedBatteryDesc}" FontSize="12" TextWrapping="Wrap" Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" Style="{StaticResource AdvancedToggleStyle}"
                                         IsChecked="{Binding SyntheticBatteryEnabled, Mode=TwoWay}"
                                         IsEnabled="{Binding SyntheticBatteryAvailable}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
